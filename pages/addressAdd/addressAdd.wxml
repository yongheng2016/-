<!--pages/addressAdd/addressAdd.wxml-->
<view>
    <view class='areaSelect'>
        <picker mode="multiSelector" bindchange="_bindRegionChange" range="{{areaArray}}" range-key='value' bindcolumnchange="_columnchange">
            <view class="picker">
                当前选择：{{region[0]}}，{{region[1]}}，{{region[2]}}
            </view>
        </picker>
    </view>
</view>